{
  "name": "ssafy-2024-frontend",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-assets": "sh -c 'mkdir -p ./dist/img && cp -r ./img/* ./dist/img'",
    "start": "rm -rf dist .parcel-cache && npm run copy-assets && parcel serve index.html",
    "build": "rm -rf dist .parcel-cache && npm run copy-assets && NODE_ENV=production parcel build index.html --public-url .",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "gh-pages": "^6.2.0",
    "parcel": "^2.13.3",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.17"
  },
  "homepage": "https://main.d236dnvke84iow.amplifyapp.com/",
  "dependencies": {
    "regenerator-runtime": "^0.14.1"
  }
}
